# Phase 7：订单评估记录原型

## 1. Phase 7 定位

Phase 7 开始从纯只读演示进入“可保存的原型数据”阶段。

但保存的是订单评估记录，不是正式销售订单。

评估记录用于回看、复盘和演示，不触发库存、采购、财务、审批等连锁业务动作。

核心原则：
实战开始，但匍匐前进。

## 2. 保存对象定义

保存对象名称：
订单评估记录。

它表示：
客户提出一个订单需求后，系统基于当前 BOM、账面库存、采购周期和仓库反馈做出的交期风险评估。

它不表示：

- 正式销售订单
- 客户合同
- 生产订单
- 采购申请
- 财务单据
- 库存占用单

## 3. 初期建议字段

记录初期可能需要的字段，但本步骤不实现代码：

- evaluationId：评估编号
- productId：产品
- plannedQty：数量
- requiredDate：期望交期
- asOfDate：分析日期
- createdAt：创建时间
- note：备注
- riskLevel：整体风险等级
- keyRiskMaterialCount：关键风险物料数量
- procurementConfirmCount：需采购确认数量
- warehouseConfirmCount：需仓库确认数量
- analysisSnapshot：分析快照

说明：
字段仅为后续设计草案，不代表本步骤新增数据模型。

## 4. 保存后不触发的动作

订单评估记录保存后，不触发以下动作：

- 不占用库存
- 不锁定库存
- 不修改库存
- 不生成采购单
- 不生成采购需求
- 不进入财务
- 不计算正式成本
- 不触发审批
- 不代表正式接单
- 不改变任何现有业务状态

## 5. 账面数据与现场库存逻辑

当前交期风险分析基于系统里的当前账面数据，包括：

- BOM
- 账面库存
- 安全库存
- 采购周期
- 已有仓库反馈

系统数据用于初始判断。

仓库确认用于复核关键物料的现场可用性。

如果账面库存与现场库存不一致，这本身属于交期风险的一部分。

系统不是假设库存数据永远正确，而是通过“需仓库确认”等提示，把关键物料的数据可信度问题提前暴露出来。

当前版本不自动修改库存。

后续如需修正库存，必须通过库存流水、盘点调整或其他正式机制单独设计。

## 6. 未来成本 / 财务链路预留

系统未来一定会与成本、采购资金和财务分析产生联系。

订单评估记录未来可能关联：

- 采购资金需求
- 采购成本
- 库存成本
- 产品成本
- 订单成本
- 毛利参考
- 财务扩展

但当前 Phase 7 初期不做：

- 正式财务核算
- 财务金额
- 应付账款
- 发票
- 付款
- 成本结转
- 财务报表

原则：
当前不乱加金额字段，不临时定义成本口径。

任何涉及金额和财务口径的功能，后续必须单独设计。

## 7. 后续建议步骤

Phase 7 后续可以按以下顺序推进：

1. 订单评估记录数据模型草案
2. 订单评估记录列表，只读展示
3. 从交期风险分析页面保存为评估记录
4. 查看历史评估记录
5. 重新分析某个评估记录
6. 再讨论是否进入库存流水、采购申请或成本参考

## 8. 当前边界

当前阶段仍然保持：

- 不保存正式订单
- 不生成采购单
- 不修改库存
- 不做权限
- 不做财务金额
- 不修改核心 MRP / 风险计算逻辑
