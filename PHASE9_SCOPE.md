# Phase 9 — Product Scope & Role Boundary Foundation

中文：产品范围与角色信息边界基础

## 1. 文档状态

- 当前步骤：`Phase 9-Step 3 - Define Phase 10 prototype surface`
- 启动日期：2026-07-20
- Step 1 完成基线：`c3a5404 define phase 9 product scope and role boundaries`
- 当前分支：`lufuta-material-system-lite`
- 第一版角色边界规则：`ROLE_ACCESS_MATRIX.md`
- Phase 10 原型范围：`PHASE10_PROTOTYPE_SCOPE.md`
- 当前性质：产品方向与边界文档，不是功能实现

Phase 9-Step 1 已把真实客户反馈、初步角色模型、信息边界原则和后续阶段顺序写清楚。Phase 9-Step 2 已把这些原则整理为可指导 Phase 10 最小原型的第一版固定规则。Phase 9-Step 3 只根据当前代码与页面盘点结果定义 Phase 10 的实施范围，不创建登录、用户、角色、权限、订单、库存、采购、生产计划、成本或财务功能，也不改变现有运行行为。

## 2. 与 Phase 8 的关系

Phase 8 已经完成并冻结一个独立可用的接单评估 / 真实数据试算演示闭环：

```text
选择数据来源
  -> 填写试算条件
  -> 运行试算
  -> 查看结论摘要、关键风险物料、角色关注点和结果明细
  -> 可选择保存接单评估记录
  -> 可复查本地保存的评估记录
```

Phase 8 不是“做了一半”，也不是等待 Phase 9 补完的残缺订单流程。它已经在既定边界内完成并冻结。接单评估记录仍不等于正式订单，试算仍不确认接单、不占用或扣减库存、不生成采购需求或采购单、不进入金额、成本或财务。

Phase 9 是在真实客户反馈基础上扩展产品方向，目的是为未来全工厂使用建立角色和信息边界基础。Phase 9 不推翻、不替换 Phase 8，也不得破坏 Phase 8 已冻结的流程和业务边界。

## 3. 真实客户反馈

系统向真实用户展示后，客户关注点已经超出订单交期、库存、缺料和风险判断，还包括：

- 系统未来是否可以在全工厂范围使用。
- 不同使用角色是否可以拥有不同权限。
- 部分角色可以查看信息，但不能创建、修改、确认或删除。
- 部分角色可以看到数量，但不能看到价值 / 金额。
- 仓库或车间不一定应该看到客户、售价、毛利等销售敏感信息。
- 同一业务链中，不同部门需要看到与自身职责相关的信息，并执行不同操作。

这些反馈说明，Lufuta 的长期产品方向不能只描述“系统有哪些页面”，还必须描述“谁可以看到什么、谁可以做什么、哪些敏感信息必须隔离”。

## 4. Phase 9 目标

Phase 9 只做三类工作：

1. 把真实客户反馈正式写入产品方向。
2. 定义第一版角色与信息边界原则。
3. 明确下一阶段先进入 `Phase 10 — Role Access Minimum Prototype`，经过桥接验证后，再进入 `Phase 11 — Formal Order Minimum Loop`。

Phase 9 的产出是可供后续原型设计使用的边界基础，不是完整权限系统，也不是正式业务执行能力。

## 5. 第一版角色模型

以下角色是第一版产品模型，用于讨论信息和操作边界。它们不是当前已经创建的系统账号或权限配置，也不代表最终组织架构已经确定。

| 角色 | 初步职责关注点 | 初步信息边界方向 |
| --- | --- | --- |
| System Admin / 系统管理员 | 系统配置、账号与角色维护、运行支持 | 未来可管理访问配置，但业务敏感数据访问仍应有明确规则和记录 |
| Management / 管理层 | 全局经营与交付风险、跨部门决策 | 可查看必要汇总和关键风险；金额、成本、毛利是否可见仍需按职责定义 |
| Sales / 销售 | 客户需求、接单评估、交期沟通 | 可查看客户和销售相关信息；不应默认获得库存、采购或成本的全部修改权限 |
| Planning / Material / 计划与物料 | 需求、BOM、物料齐套、交期可行性 | 以数量、需求和风险信息为主；金额、售价和毛利不应默认可见 |
| Purchasing / 采购 | 缺料、采购周期、供应商与采购执行 | 可查看完成采购职责所需信息；销售价格、客户敏感信息和毛利不应默认可见 |
| Warehouse / 仓库 | 库存数量、库位、收发与复核 | 以库存和物料操作信息为主；客户、售价、采购价格、成本和毛利不应默认可见 |
| Production / Workshop / 生产或车间 | 生产需求、领料、进度与异常反馈 | 以生产和物料数量信息为主；销售敏感信息、采购价格、成本和毛利不应默认可见 |
| Finance / Cost / 财务与成本 | 金额、成本、核算与财务控制 | 可处理职责范围内的金额与成本信息；业务操作权限应与财务职责分开定义 |
| Read-only / Audit / 只读或审计 | 查询、复核、审计和追溯 | 原则上只查看授权范围，不创建、修改、确认或删除业务数据 |

一个用户未来可能拥有多个角色。多角色如何组合、敏感字段限制如何优先、临时授权如何到期，都需要在后续阶段单独设计；Phase 9 不实现角色合并算法或权限配置界面。

## 6. 第一版权限与信息边界原则

### 6.1 模块访问权限

未来应按角色定义可进入的模块，例如销售、接单评估、物料 / BOM、计划、采购、仓库、生产、管理、财务 / 成本和审计。能够进入系统不代表能够访问全部模块。

### 6.2 操作权限

模块访问与操作权限必须分开。未来至少需要区分：

- 查看
- 创建
- 修改
- 确认
- 删除
- 导出

“可以查看”不自动代表“可以修改”，“可以修改”也不自动代表“可以确认、删除或导出”。关键业务动作还需要在对应业务阶段定义状态、责任、撤销和审计边界。

### 6.3 数据范围权限

未来需要定义用户能够访问的数据范围，例如全部数据、所属部门、负责客户、负责订单、指定仓库、指定车间或明确授权范围。Phase 9 只确认需要数据范围边界，不建立组织架构或过滤引擎。

### 6.4 字段可见权限

能够查看一条业务记录，不代表能够查看其中所有字段。未来需要区分普通业务字段与敏感字段，并在服务端数据访问和页面展示两侧保持一致边界；不能只依赖前端隐藏。

### 6.5 数量与金额分离

数量、需求、库存、缺料和交期风险可以服务于计划、采购、仓库和生产协作；售价、采购价格、成本、毛利和其他金额字段属于另一类敏感信息。未来应允许角色查看数量而不查看价值 / 金额。

### 6.6 销售敏感信息边界

客户身份、客户价格、销售价格、销售条款、毛利以及其他销售敏感字段不应默认向仓库或车间开放。采购、计划与物料等角色也只应看到完成职责所需的信息。

### 6.7 采购价格、成本和毛利边界

采购价格、供应商商业条件、标准成本、实际成本和毛利等字段应分别定义可见角色。拥有采购操作权限不自动代表可以查看销售价格或毛利；拥有数量查看权限也不自动代表可以查看采购价格或成本。

### 6.8 多角色原则

一个用户未来可能同时承担多个角色。后续设计必须明确多角色权限组合、敏感字段限制优先级和职责冲突处理，不能简单假设角色越多即可看到全部信息。

### 6.9 关键操作记录原则

未来的确认、删除、状态转换、库存影响、采购执行和其他关键操作，需要至少记录操作者和操作时间，并根据业务风险补充操作对象、前后状态和原因。Phase 9 只建立原则，不开发完整操作日志系统。

## 7. 后续阶段顺序

后续工作必须按以下顺序推进，不能从 Phase 9 直接跳到正式订单：

### Phase 9 — Product Scope & Role Boundary Foundation

明确产品范围、初步角色模型、权限与信息边界原则。本阶段以文档和边界确认为主。

### Phase 10 — Role Access Minimum Prototype

Phase 9-Step 3 已在 `PHASE10_PROTOTYPE_SCOPE.md` 中定义第一版页面、现有数据、验证方式和技术边界，但没有启动实现。只有 Product Owner 单独确认进入 Phase 10 后，才可按该边界实现最小角色访问原型。

### Phase 10 完成前桥接验证

进入 Phase 11 前必须验证：

- 角色边界是否合理。
- 接单评估到正式订单的转换点是否清楚。
- 正式订单最小数据结构是否明确。
- Phase 8 原流程是否仍正常。

桥接验证用于判断是否具备进入正式订单最小闭环的条件，不代表已经转换订单、占用库存或生成采购业务。

### Phase 11 — Formal Order Minimum Loop

只有桥接验证通过，且 Product Owner 单独确认 Phase 11 的数据、状态、责任、回退、审计和测试边界后，才可进入正式订单最小闭环。Phase 9 不设计或实现 Phase 11 功能。

## 8. Phase 9 明确不做

- 不开发完整登录系统。
- 不开发用户数据库。
- 不开发复杂角色配置器。
- 不开发逐字段权限编辑器。
- 不开发多工厂组织架构。
- 不开发完整审批流。
- 不开发完整操作日志系统。
- 不开发正式订单。
- 不占用、保留、锁定或扣减库存。
- 不生成采购需求或采购单。
- 不进入金额、成本或财务功能。
- 不新增 Excel / CSV / PDF 导入。
- 不修改核心 MRP / 风险计算逻辑。
- 不推翻或改写 Phase 8 已冻结的演示闭环。
- 不进入 Phase 10 或 Phase 11 实现。

## 9. Phase 9-Step 1 完成标准

- 真实客户反馈已写入产品方向。
- 第一版角色模型已记录，并明确它不是完整权限系统实现。
- 模块、操作、数据范围和字段可见权限原则已记录。
- 数量与金额、销售敏感信息、采购价格 / 成本 / 毛利的边界方向已记录。
- 多角色用户和关键操作记录原则已记录。
- Phase 9、Phase 10、桥接验证、Phase 11 的顺序已记录。
- Phase 8 的独立完成状态和冻结边界已保留。
- 本步骤只修改文档，不修改业务代码、数据模型或测试。

## 10. Phase 9-Step 2 第一版最小规则

Phase 9-Step 2 将 Phase 10 最小原型的优先角色收敛为：

- Management / 管理层
- Sales / 销售
- Purchasing / 采购
- Warehouse / 仓库
- Production / Workshop / 生产或车间

System Admin、Planning / Material、Finance / Cost、Read-only / Audit 及其他未来角色保留为后续扩展，不是 Phase 10 第一版必须实现的对象。Phase 10 只需要足够验证不同角色看到不同信息、拥有不同操作权限，不需要一次建立完整组织架构。

第一版规则使用以下五个权限维度：

- Module Access
- View
- Edit
- Sensitive Field Visibility
- Action Permission

敏感信息按 Customer-sensitive、Sales-sensitive、Supplier-sensitive、Price / Cost / Margin、Internal Notes、Quantity、Operational Status 分类。数量可见不代表价值 / 金额可见；同一条业务记录可以按角色显示不同字段；敏感字段隐藏后仍必须保留完成角色职责所需的信息。

各角色的 Can View、Can Act、Must Not See、固定动作权限、敏感信息处理和 Phase 10 验证标准统一记录在 `ROLE_ACCESS_MATRIX.md`。该规则不设计复杂 RBAC 引擎、动态角色配置器、逐字段权限界面或权限表达式语言。

Phase 10 尚未开始。Phase 8 保持冻结，权限层未来只能影响“看什么 / 能做什么”，不能改变相同输入下的 MRP 和风险计算结果。

## 11. Phase 9-Step 2 完成标准

- Phase 10 第一版优先角色和后续扩展角色已分层。
- 每个优先角色的 Can View、Can Act、Must Not See 已定义。
- Module Access、View、Edit、Sensitive Field Visibility、Action Permission 已形成最小固定模型。
- 第一版敏感信息分类和数量 / 金额分离原则已定义。
- Phase 10 的六项最小验证目标和明确不解决的问题已记录。
- Phase 8 冻结边界和 MRP / 风险计算一致性要求已保留。
- Phase 10 尚未开始，后续阶段顺序未改变。
- 本步骤只修改文档，不修改业务代码、数据模型或测试。

## 12. Phase 9-Step 3 原型范围决定

Phase 10 第一版只以“真实数据试算”作为核心验证页面。该页面已有数据来源选择、试算输入、运行试算、结果摘要、风险结果、缺料信息、采购周期、采购建议、仓库确认点和本地接单评估记录，已经足够验证 View、Edit、Action Permission 和 Sensitive Information Boundary。其他页面只作为未来补充或验证参考，不是首批必做范围。

五个最小角色继续保持为 Management、Sales、Purchasing、Warehouse、Production / Workshop。Sales 可使用 Phase 8 已有的输入、运行、保存和本地摘要查看动作；其余角色按 `ROLE_ACCESS_MATRIX.md` 和 `PHASE10_PROTOTYPE_SCOPE.md` 使用不同的只读信息范围。任何角色都不得因此获得正式订单、库存执行、采购执行或生产执行能力。

第一版不新增模拟敏感字段，优先使用 Phase 8 现有数据和现有演示询单备注。`Demo Role Switcher` 只可使用固定五角色和前端临时状态，刷新后恢复默认角色；它不包含登录、账号、密码、用户数据库或后端权限同步，也不是正式安全权限系统。

预计技术范围最多为 `src/app.js`、`src/styles.css`、可选的 `src/roleAccess.js` 和新增的 `tests/roleAccess.test.js`。未经独立批准，不得修改 `src/data.js`、`src/mrp.js` 或 `src/planning/*`。

相同业务输入在所有角色下必须产生完全一致的 MRP、风险、缺料和采购建议计算结果。角色权限只能改变区域、字段和现有动作的可见或可用状态。完整实施边界记录在 `PHASE10_PROTOTYPE_SCOPE.md`。

## 13. Phase 9-Step 3 状态

- 本步骤是 Phase 10 实施范围定义，不是权限功能实现。
- Phase 10 尚未开始，Phase 8 继续保持冻结。
- Phase 9 的规划目标至此基本完成。
- Phase 10 完成后仍必须进行四项桥接验证，验证通过并由 Product Owner 单独确认后，才可进入 Phase 11。
- 本步骤只修改文档，不修改业务代码、数据模型或测试。
