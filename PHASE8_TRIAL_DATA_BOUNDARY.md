# Phase 8-Step 5: 临时试算数据 vs 正式基础数据边界定义

## 一、文档目的

本文件用于定义 Phase 8 真实数据试算中两类数据的边界：

1. 临时试算数据
2. 正式基础数据

核心原则：

临时试算数据只用于本次试算，不写入正式物料、BOM、库存、价格或财务资料。

正式基础数据会被系统长期引用，因此进入系统前必须经过确认和维护流程。

## 二、临时试算数据定义

临时试算数据，是指用户为了本次接单判断临时输入或导入的数据。

这些数据可能来自：

- 客户 BOM
- 临时库存表
- 供应商口头报价
- 采购临时确认的交期
- 手工录入的试算条件

临时试算数据的特点：

- 只用于当前一次试算
- 可以不完整
- 可以带有待确认状态
- 可以包含临时价格或临时采购周期
- 不作为正式物料资料
- 不作为正式 BOM
- 不作为正式库存
- 不作为正式采购价格
- 不作为正式财务依据

## 三、正式基础数据定义

正式基础数据，是指系统长期维护、长期引用，并可能影响多个业务判断的数据。

正式基础数据包括：

- 正式物料资料
- 正式产品 / BOM 资料
- 正式库存台账
- 正式供应商资料
- 正式采购周期
- 正式参考价格资料
- 未来可能扩展的正式成本资料

正式基础数据的特点：

- 会被多个订单或试算重复引用
- 会影响库存判断
- 会影响采购建议
- 会影响未来成本参考
- 可能影响后续财务扩展
- 需要维护、确认、版本管理或审批

## 四、两类数据的关键区别

临时试算数据：

- 用于一次性判断
- 不长期保存为正式资料
- 不覆盖系统资料
- 不改变库存
- 不生成采购单
- 不进入财务
- 可带有“待确认”状态

正式基础数据：

- 用于长期系统运行
- 会被后续订单引用
- 可能影响库存、采购、成本
- 需要正式维护流程
- 可能需要权限或审核
- 未来可连接财务模块

## 五、导入数据的未来路径

未来导入数据时，必须先判断导入目的。

### 1. 作为临时试算数据导入

- 只用于本次试算
- 不覆盖正式资料
- 试算完成后可丢弃或另存为评估记录
- 不能直接修改库存或价格

### 2. 作为正式基础数据导入

- 需要字段校验
- 需要人工确认
- 需要版本管理
- 需要防止覆盖已有数据
- 未来可能需要审批或权限

## 六、BOM 数据边界

临时 BOM：

- 可以来自客户文件或临时 Excel
- 只用于本次试算
- 不覆盖正式产品 BOM
- 可以存在物料名称不完整、编码缺失、单位待确认等问题

正式 BOM：

- 必须绑定产品
- 必须有明确物料编码
- 必须有单位用量
- 应考虑版本管理
- 会影响后续订单、采购和库存判断

## 七、库存数据边界

临时库存：

- 可以来自临时盘点表
- 可以用于本次试算参考
- 不修改系统库存台账
- 不生成库存流水
- 不代表正式库存结余

正式库存：

- 来自系统库存台账
- 未来应由入库、领料、退货、盘点等动作更新
- 会影响缺料判断和采购建议
- 不应被临时试算直接覆盖

## 八、价格与成本数据边界

临时价格：

- 可以来自供应商口头报价、历史估价、临时参考价
- 只用于采购金额参考
- 不作为正式采购价
- 不作为正式成本
- 不进入财务
- 不生成应付账款
- 不影响库存入账金额

正式价格 / 成本：

- 未来可能来自供应商报价、采购订单、入库成本、运费、关税等
- 需要区分参考价、实际采购价、含税价、不含税价
- 可能影响产品成本、订单成本、毛利参考
- 未来如进入财务，必须与试算结果分离

## 九、订单试算结果边界

试算结果可以输出：

- 缺料结果
- 交期风险
- 采购建议
- 采购金额参考
- 成本影响参考
- 仓库确认点

但试算结果不会自动：

- 保存正式订单
- 占用库存
- 扣减库存
- 生成采购单
- 生成付款申请
- 进入应付账款
- 进入正式财务
- 生成财务凭证
- 更新正式 BOM
- 更新正式库存
- 更新正式价格

## 十、未来扩展建议

后续可以分阶段实现：

- 先支持选择系统现有产品做一次性试算
- 再支持临时输入订单条件
- 再支持临时 BOM 数据
- 再支持临时库存数据
- 再支持参考价格状态
- 再支持少量演示参考价格
- 再支持采购金额参考
- 最后再考虑正式导入和基础资料维护流程

## 十一、当前阶段限制

Phase 8-Step 5 只做文档。

当前阶段不做：

- 不修改 src/app.js
- 不修改 src/data.js
- 不修改 src/mrp.js
- 不修改 tests
- 不新增页面
- 不新增导航
- 不新增输入
- 不新增导入
- 不新增保存
- 不新增计算
- 不新增采购逻辑
- 不新增库存逻辑
- 不新增财务逻辑
- 不进入 Phase 8-Step 6
