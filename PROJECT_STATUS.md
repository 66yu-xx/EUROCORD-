# Project Status

## Phase 9-Step 2 — Define minimum role and information boundary rules - 2026-07-20

- 当前阶段：`Phase 9-Step 2 - Define minimum role and information boundary rules`
- 启动基线：`c3a5404 define phase 9 product scope and role boundaries`
- 当前分支：`lufuta-material-system-lite`
- 当前测试基线：`node --test tests/*.test.js`，51/51 pass
- 角色边界规则：`ROLE_ACCESS_MATRIX.md`
- 本步骤只设计规则，不实现登录、用户、角色或权限功能。

Phase 10 最小原型的第一版优先角色确定为 Management / 管理层、Sales / 销售、Purchasing / 采购、Warehouse / 仓库、Production / Workshop / 生产或车间。System Admin、Planning / Material、Finance / Cost、Read-only / Audit 及其他未来角色保留为后续扩展，不是 Phase 10 的首批必做对象。

第一版规则为每个优先角色定义 Module Access、View、Edit、Sensitive Field Visibility 和 Action Permission，并分别记录 Can View、Can Act、Must Not See。规则只基于 Phase 8 已存在的真实数据试算、风险结果和本地接单评估记录；当前可执行动作必须基于 Phase 8 已有能力。尚不存在的客户、销售、供应商、正式生产计划、价格、成本、毛利等敏感字段，只能作为未来信息边界或模拟权限概念；Phase 10 不得因此新增正式业务模块或数据模型，也不得假设正式订单、采购单、库存执行或财务模块已经存在。

敏感信息分为 Customer-sensitive、Sales-sensitive、Supplier-sensitive、Price / Cost / Margin、Internal Notes、Quantity 和 Operational Status。数量可见与价值 / 金额可见是两个不同权限维度；同一条业务记录可以按角色显示不同字段，但隐藏敏感字段不能妨碍角色完成本职工作。

Phase 10 只需要验证不同角色看到不同内容、只读与可操作状态可区分、敏感字段隐藏后页面仍可用、同一数据可形成不同角色视图，以及权限层不改变 Phase 8 的 MRP 和风险计算结果。Phase 10 尚未开始，Phase 8 仍保持冻结。

后续顺序保持为：Phase 9 → Phase 10 最小权限原型 → 桥接验证 → Phase 11 正式订单最小闭环。

## Phase 9-Step 1 — Document product scope and role boundary direction - 2026-07-20

- 当前阶段：`Phase 9-Step 1 - Document product scope and role boundary direction`
- 中文定位：`产品范围与角色信息边界基础`
- 启动基线：`40db6b6 freeze phase 8 real data trial flow`
- 当前分支：`lufuta-material-system-lite`
- 当前测试基线：`node --test tests/*.test.js`，51/51 pass
- 详细范围文档：`PHASE9_SCOPE.md`
- 本步骤只更新项目方向与边界文档，不实现功能代码。

真实用户展示后的反馈说明，产品关注点已经从订单交期、库存和风险判断扩展到全工厂使用范围与角色信息边界。客户关心不同角色是否可以拥有不同权限，包括只查看不修改、查看数量但不查看价值 / 金额、限制仓库或车间查看销售敏感信息，以及让不同部门在同一业务链中看到不同信息并执行不同操作。

Phase 9 只做三类工作：

1. 把真实客户反馈正式写入产品方向。
2. 定义第一版角色与信息边界原则。
3. 明确后续顺序为 `Phase 10 — Role Access Minimum Prototype`，完成桥接验证后，再进入 `Phase 11 — Formal Order Minimum Loop`。

第一版角色方向包括系统管理员、管理层、销售、计划与物料、采购、仓库、生产或车间、财务与成本、只读或审计。以上只是初步角色模型，不代表当前已经实现登录、用户、角色或完整权限系统。

Phase 9 当前只定义模块访问、操作、数据范围和字段可见原则，明确数量与金额分离、销售敏感信息边界、采购价格 / 成本 / 毛利等敏感字段边界、多角色用户方向，以及未来关键操作需要记录操作者和时间。Phase 9 不开发正式订单，不改变库存，不生成采购需求或采购单，不进入金额、成本或财务，不新增导入，也不修改核心 MRP / 风险计算逻辑。

Phase 8 不是未完成的中间状态。Phase 8 已完成并冻结一个独立可用的真实数据试算 / 接单评估演示闭环。Phase 9 是基于真实客户反馈扩展产品方向，不推翻、不替换 Phase 8，也不改变其冻结业务边界。

Phase 10 尚未开始。Phase 11 尚未开始。

## Phase 8 Freeze - 2026-06-29

- 当前稳定基线：`4b6ac1e scroll to trial results after run`
- 当前分支：`lufuta-material-system-lite`
- 测试基线：`node --test tests/*.test.js`，51/51 pass
- 远端同步状态：0 ahead / 0 behind
- 冻结文档：`PHASE8_FREEZE.md`

Phase 8 已冻结为“真实数据试算 / 接单评估记录演示路径”基线。当前演示路径为：选择数据来源 → 填写试算条件 → 运行试算 → 自动滚动到结果区 → 查看下一步建议 → 查看试算结论摘要 → 查看关键风险物料 → 查看角色关注点 → 查看结果明细 → 可选择保存接单评估记录 → 可在已保存评估记录中复查。

Step 15 和 Step 15B 已完成冻结前体验收口：运行试算后提供下一步建议，保存后提示可复查，点击运行试算后自动定位到结果区。以上引导均为轻引导，不强制用户按顺序操作。

Phase 8 冻结边界：

- 当前仍然不是正式订单系统。
- 接单评估记录不等于正式订单。
- 不确认接单。
- 不作废业务记录。
- 不转订单。
- 不占用、锁定、保留或扣减库存。
- 不生成采购需求或采购单。
- 不计算金额。
- 不进入财务 / 成本。
- 不新增导入 Excel / CSV / PDF。
- 不修改核心 MRP / 风险计算逻辑。
- 不自动保存接单评估记录。

Phase 8-Freeze 只固化当前阶段文档和状态。冻结完成后今天停止，不自动进入 Phase 9 或任何下一阶段。

## Phase 5 Freeze - 2026-06-27

- 当前稳定基线：`751fd73 clarify warehouse feedback role flow`
- 当前分支：`lufuta-material-system-lite`
- 测试基线：`node --test tests/*.test.js`，47/47 pass
- 远端同步状态：0 ahead / 0 behind

Phase 5 已冻结为“仓库侧库存预警反馈”的只读演示基线。仓库反馈是库存可信度信号，不是真实库存操作系统；系统库存仍是分析基础，仓库只反馈现场库存是否可信、是否需要人工复核。

已完成内容：

- Phase 5-Step 1：定义仓库只读 / 反馈型角色边界，仓库只反馈现场库存可信度，不直接修改系统库存，不生成采购单，不确认采购建议，不影响 MRP 核心计算结果。
- Phase 5-Step 2：整理“库存预警与仓库反馈（只读）”页面结构，展示物料编码、物料名称、单位、系统库存、安全库存、库存状态、仓库反馈状态，以及给老板 / 计划 / 采购的提示说明。
- Phase 5-Step 3：增加仓库反馈状态解释层，解释未确认、已确认、现场异常、待盘点，并明确这些状态只是库存可信度信号。
- Phase 5-Step 4：补强四角色信息流说明，帮助老板、计划、采购、仓库理解仓库反馈状态在信息链中的作用。

Phase 5 冻结边界：

- 不保存反馈。
- 不提交反馈。
- 不写入 localStorage。
- 不改变库存。
- 不影响 MRP 核心计算。
- 不改变交期风险计算。
- 不改变采购建议。
- 不生成采购单。
- 不确认采购建议。
- 不新增权限系统。
- 不新增财务金额。
- 当前系统仍为只读分析与演示闭环，入库、领料、退货仍为占位 / 演示边界。

Phase 5 后续可能方向：

- 未来可以考虑真实仓库反馈记录。
- 未来可以考虑盘点异常记录。
- 未来可以考虑反馈状态与老板 / 计划 / 采购页面联动。
- 未来可以考虑权限和审核流。
- 以上方向都不属于当前 Phase 5 冻结范围，必须单独确认后才能进入设计或实现。

Phase 6 不会自动开始；后续是否进入下一阶段需单独确认范围。

## Phase 4 Freeze - 2026-06-26

- 当前稳定基线：`c663879 add table scroll hint`
- 当前分支：`lufuta-material-system-lite`
- 测试基线：`node --test tests/*.test.js`，47/47 pass
- 远端同步状态：0 ahead / 0 behind

Phase 4 已冻结为演示可读性与移动端稳定性基线，不是业务功能扩展。本阶段完成移动端和演示可读性检查，已在 375px / 390px 下检查 10 个页面，所有页面 `pageOverflowX = 0`，表格内部横向滚动保持正常，并新增移动端表格轻提示：

```text
提示：表格可左右滑动查看更多字段
```

Phase 4 边界：

- 未修改 MRP 核心计算逻辑。
- 未新增真实库存操作。
- 未新增保存行为。
- 未新增采购单生成。
- 未新增权限系统。
- 未新增财务金额。
- 入库、领料、退货仍为占位 / 演示边界。
- 当前系统仍是只读分析与演示闭环。

Phase 5 可讨论仓库侧反馈能力，例如库存预警反馈、盘点异常记录、补货观察入口，以及仓库只读 / 反馈型角色边界。进入 Phase 5 前仍需单独确认范围，不能从 Phase 4 Freeze 自动延伸为业务执行能力。

## 历史状态：V3 / Phase 4-Step 1

- 项目：`Lufuta Material Management System Lite`
- 当时阶段：V3 / Phase 4-Step 1 — 系统演示路径设计
- 当前分支：`lufuta-material-system-lite`
- 当前稳定基线：`b9da4e8 polish warehouse feedback read-only copy`
- 当前测试基线：47/47 通过
- 远端同步状态：0 ahead / 0 behind
- 当前目标：设计系统演示阅读路径，只更新项目文档，不修改运行逻辑。

## Phase 2A 已完成能力（已冻结）

交期风险分析页面已形成完整的只读分析闭环：

```text
产品选择
    → 计划数量
    → requiredDate
    → asOfDate
    → BOM 需求
    → 库存缺口
    → 采购周期
    → 交期风险等级
    → 分析摘要与说明
```

- 根据产品 BOM 计算计划需求。
- 根据库存判断物料缺口。
- 根据物料采购周期判断交期风险。
- 展示交期风险等级。
- 展示分析条件摘要和风险等级说明。
- 保持原有表格字段和风险结果不变。

## Phase 2B 已完成能力

Phase 2B 已形成只读采购建议闭环：

```text
交期风险分析
    → 物料风险判断
    → 采购建议生成
    → 采购建议展示
    → 明确只读边界
```

- 完成采购建议规则与数据结构审查。
- 新增只读采购建议纯计算规则。
- 将 `recommendation` 接入 `deliveryRiskPreview.rows` 运行期结果。
- 在交期风险分析结果下方展示“采购建议视图（只读）”。
- 建议基于当前计划数量、BOM、库存、安全库存和采购周期自动生成。
- 展示建议动作、建议采购数量、原因、优先级、采购周期和风险等级。

## Phase 2C 已完成能力

Phase 2C 已将当前页面收口为“下单前交期风险与采购建议分析页面”，面向老板、计划、采购三类阅读对象。仓库角色暂不作为当前页面主要使用对象，后续可单独设计库存预警 / 补货反馈 / 盘点异常视图。

当前页面已经形成：

1. 分析条件摘要：说明本次分析的订单条件。
2. 订单决策摘要：面向老板，快速判断订单风险、主要原因和建议动作。
3. 交期可行性说明：面向计划，解释期望交期、分析日期、剩余天数、关键采购周期、影响物料和计划判断。
4. 采购优先级分组：面向采购，分为立即确认、建议关注、暂不采购。
5. 风险结果 / 采购建议表格：作为详细数据依据。

- Phase 2C-Step 1：新增订单决策摘要，面向老板视角。
- Phase 2C-Step 2：新增交期可行性说明，面向计划视角。
- Phase 2C-Step 3：新增采购优先级分组，面向采购视角。
- Phase 2C 收尾审查：优化页面定位、老板→计划→采购阅读顺序、标题、风险口径和只读边界文案。

## 当前业务边界

交期风险分析与采购建议页面仅用于只读判断参考：

- 不保存正式订单。
- 不生成采购单。
- 不保存采购建议。
- 不修改或占用库存，也不进入库存过账。
- 不做采购执行。
- 不连接 API 或数据库。
- 不做 Dashboard 汇总。
- 不恢复旧 `ordersPage` / `analysisPage` 业务路线。
- 不实现登录、权限或多公司能力。
- 不涉及财务金额。
- 实际采购仍需人工确认。

## Phase 2A 冻结点

- V3 / Phase 2A-Step 13 已完成并提交：`e91e33c polish delivery risk analysis results`。
- 功能范围稳定：只读分析闭环已完成，业务边界明确。
- 提交状态稳定：Step 13 已提交，工作树在 Step 14 开始前保持干净。
- 远端状态稳定：本地与 `origin/lufuta-material-system-lite` 同步，0 ahead / 0 behind。
- 测试基线稳定：40/40 通过。

## Phase 2B 冻结点

- V3 / Phase 2B-Step 5 已完成并提交：`3555dbf clarify read-only procurement recommendation boundaries`。
- 功能范围稳定：只读采购建议的计算、运行期接入、展示和边界说明均已完成。
- 数据边界稳定：建议只存在于页面运行期，不保存订单或建议，不生成采购单，不修改或占用库存。
- 远端状态稳定：本地与 `origin/lufuta-material-system-lite` 同步，0 ahead / 0 behind。
- 测试基线稳定：47/47 通过。

## Phase 2C 冻结点

- V3 / Phase 2C 当前稳定基线：`c76f402 freeze phase 2c analysis documentation baseline`。
- 功能范围稳定：只读分析页面已形成老板→计划→采购的阅读顺序，并保留详细风险结果 / 采购建议表格作为数据依据。
- 页面定位稳定：当前页面用于下单前交期风险与采购建议分析，不承担仓库日常操作、采购执行或订单保存职责。
- 数据边界稳定：页面不保存订单、不生成采购单、不修改库存、不做采购执行、不做权限登录、不涉及财务金额。
- 远端状态稳定：本地与 `origin/lufuta-material-system-lite` 同步，0 ahead / 0 behind。
- 测试基线稳定：47/47 通过。

## Phase 3 已完成能力

Phase 3 已形成仓库视角的“库存预警与仓库反馈（只读）”页面。页面定位为发现、提示、反馈，不执行真实库存动作。

Phase 3 为仓库角色提供只读视图，帮助仓库识别：

1. 哪些物料库存已经不足或低于安全库存。
2. 哪些物料需要提前提醒采购关注。
3. 哪些库存数字可能需要复查。
4. 哪些物料可能影响后续订单交期判断。

仓库角色定位：

- 仓库不是决定是否接单的人。
- 仓库不是采购执行人。
- 仓库不是财务审批人。
- 仓库的价值是提供库存状态、库存风险和库存可靠性反馈。

当前页面包含四个基础区块：

1. 仓库库存概览。
2. 库存预警列表。
3. 仓库复查提示。
4. 对计划 / 采购的反馈提示。

Phase 3 不做：

- 不做真实入库。
- 不做真实领料。
- 不扣减库存。
- 不保存盘点或复查结果。
- 不生成采购单。
- 不修改订单。
- 不修改采购建议逻辑。
- 不修改 Phase 2C 交期风险分析逻辑。
- 不做权限登录。
- 不做采购执行流程。
- 不涉及财务金额。

## Phase 3 冻结点

- V3 / Phase 3 当前稳定基线：`b9da4e8 polish warehouse feedback read-only copy`。
- 功能范围稳定：“库存预警与仓库反馈（只读）”页面已形成仓库只读反馈闭环。
- 页面结构稳定：仓库库存概览 → 库存预警列表 → 仓库复查提示 → 对计划 / 采购的反馈提示。
- 页面定位稳定：仓库页面只负责发现、提示、反馈，不执行真实库存动作。
- 数据边界稳定：页面不做真实入库、真实领料、库存扣减、盘点或复查结果保存、采购单生成、权限登录或财务金额处理。
- 逻辑边界稳定：页面不修改 MRP、Phase 2C 交期风险分析或采购建议逻辑。
- 远端状态稳定：本地与 `origin/lufuta-material-system-lite` 同步，0 ahead / 0 behind。
- 测试基线稳定：47/47 通过。

## Phase 4-Step 1 演示路径设计

Phase 4-Step 1 只做系统演示路径规划，不修改 UI、导航、代码或运行逻辑。目标是让当前 Demo 更适合给老板 / 朋友说明系统价值、角色阅读顺序和当前边界。

推荐演示顺序：

1. 首页：说明系统定位为订单驱动的物料风险分析 Demo，不是真实 ERP、WMS 或财务系统。
2. 交期风险分析：给老板、计划、采购阅读；老板看订单是否会因为缺料等待，计划看交期是否可行，采购看哪些物料需要优先确认。
3. 库存预警与仓库反馈：给仓库、计划、采购阅读；仓库看库存预警和复查提示，计划 / 采购看仓库反馈提示。该页面只做发现、提示、反馈，不执行真实库存动作。
4. 库存台账 / 待审核流水 / 入库 / 领料 / 供应商退货：作为基础资料或后续执行模块占位，不作为当前演示核心，不代表真实业务已完成。

当前演示核心页面：

- 首页。
- 交期风险分析。
- 库存预警与仓库反馈（只读）。

基础资料或占位页面：

- 物料资料、产品 / BOM、库存台账。
- 待审核流水、入库、领料、供应商退货。

Phase 4-Step 1 边界：

- 当前系统仍是只读分析 / 演示系统。
- 不做真实库存执行、采购单、生产单、权限登录或财务金额。
- 不修改 MRP、Phase 2C 交期风险分析、采购建议或库存计算逻辑。
- 不进入生产视角页面开发。

## Phase 3-Step 1 信息结构设计

Phase 3-Step 1 只定义“库存预警与仓库反馈”页面的信息结构。页面仍然是只读视图，仓库提供库存状态反馈，不做业务执行；当前不会保存任何仓库操作结果，也不会改变库存数据。

建议页面区块：

1. 仓库库存概览：用于给仓库快速看整体风险，可包含当前物料总数、库存不足数量、低于安全库存数量、建议复查数量、关键物料风险数量。
2. 库存预警列表：用于显示需要仓库关注的物料，可包含物料编码、物料名称、当前库存、安全库存、库存状态、风险原因。
3. 仓库复查建议：用于提示哪些库存数字需要仓库复查；这一步只定义复查建议，不保存复查结果。
4. 对计划 / 采购的反馈提示：用于说明哪些物料可能影响交期判断准确性，哪些物料建议采购提前关注；仓库页面不直接决定采购，不生成采购单，不修改采购建议。

库存预警列表的风险原因示例：

- 已缺料。
- 低于安全库存。
- 库存刚好够用。
- 关键物料库存偏低。

仓库复查建议的原因示例：

- 库存数量为 0。
- 库存低于安全库存。
- 当前库存刚好等于需求边界。
- 关键物料库存很低。
- 有退货 / 历史遗留 / 替代料风险的物料。

## 下一步

Phase 4-Step 1 当前仅完成演示路径设计。下一步应先确认是否进入首页 / 导航 / 页面说明的轻量演示整理；未经确认，不修改代码、不扩展库存操作流程、不新增入库、领料、盘点保存、采购执行、订单修改、权限登录或财务金额能力。
