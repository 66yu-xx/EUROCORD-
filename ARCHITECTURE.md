# Architecture

## 架构目标

`Lufuta Material Management System Lite` 是一个统一入口的网页物料管理系统。目标生产架构为：

```text
浏览器前端
    ↓
后台 API
    ↓
服务器数据库
```

朋友提供的 Excel/VBA 系统是业务蓝图，不是目标技术架构。系统不得复制其多文件、多模板、多按钮的实现方式。

## 应用分层

无论当前使用简化存储，还是未来接入服务器数据库，代码结构都必须保持：

```text
UI 层
    ↓
service / business logic 层
    ↓
storage / data access 层
```

### UI 层

- 负责页面展示、用户输入和交互反馈。
- 调用 service 层完成业务操作。
- 禁止直接读写 `LocalStorage`、数据库或其他具体存储实现。

### service / business logic 层

- 负责业务规则、数据校验、流程编排和领域计算。
- 对 UI 提供稳定的业务接口。
- 不依赖具体页面结构，也不把具体存储细节泄漏给 UI。

### storage / data access 层

- 负责数据读取、保存、查询和存储适配。
- 当前可封装 `LocalStorage` 或其他简化 storage，作为原型实现。
- 未来可替换为后台 API 客户端，而不要求大规模重写 UI 和业务逻辑。

## 当前阶段存储策略

- 当前阶段允许使用前端本地存储等简化 storage，以支持快速验证。
- 页面不能直接和 `LocalStorage` 或具体存储方式深度绑定。
- 数据模型、service 接口和 storage 接口应为未来 API 化预留清晰边界。
- 本地存储只用于原型，不承担生产环境的数据一致性、并发、权限或备份职责。

## V3 / Phase 2A—2C 只读分析定位

- 当前页面是“下单前交期风险与采购建议分析页面”，是只读计算与展示入口。
- 页面面向老板、计划、采购：老板先看订单决策摘要，计划再看交期可行性说明，采购最后看采购优先级分组和明细表格。
- 仓库角色暂不作为当前页面主要使用对象，后续可单独设计库存预警 / 补货反馈 / 盘点异常视图。
- 页面依据产品、计划数量、`requiredDate` 和 `asOfDate` 汇总 BOM 需求，结合库存缺口与物料采购周期计算交期风险。
- Phase 2B 使用纯计算规则，根据计划数量、BOM、库存、安全库存和采购周期，为每项物料生成运行期 `recommendation`。
- Phase 2C 在页面中形成分析条件摘要、订单决策摘要、交期可行性说明、采购优先级分组、风险结果 / 采购建议表格。
- 页面展示只读采购建议；建议不进入 storage，不形成采购单或其他业务单据。
- 当前分析不保存采购建议或正式订单、不生成采购单、不修改、占用或过账库存，不做采购执行、权限登录或财务金额，也不连接 API / 数据库或汇总到 Dashboard。
- 采购建议仅供判断参考，实际采购仍需人工确认。
- 旧 `ordersPage` / `analysisPage` 不在本阶段恢复。

## Phase 3 仓库视角边界

- Phase 3 候选页面定位为“库存预警与仓库反馈”，属于仓库视角只读风险视图，不是库存执行模块。
- 仓库视角可读取库存状态、库存风险和库存可靠性线索，用于提醒库存不足、低于安全库存、需要复查的库存数字，以及可能影响后续订单交期判断的物料。
- 仓库不负责决定是否接单，不负责采购执行，也不负责财务审批。
- 当前阶段不做真实入库、真实领料、库存扣减、盘点结果保存、采购单生成、订单修改、权限登录、采购执行流程或财务金额。
- 该方向目前只做文档设计准备，不新增 UI、service、storage 或测试实现。

## 最终部署方式

- 系统部署在工厂内网服务器。
- 老板、仓库、采购、生产等部门的电脑通过浏览器访问。
- 数据集中存储在服务器数据库中，由后台 API 统一访问。
- Excel 只作为导入 / 导出入口，不作为主数据库。

## 当前架构边界

当前不设计复杂权限、审批、财务、采购合同、PDF 归档或多用户登录。后续在核心物料和库存流程稳定后，再依据真实工厂需求逐步增强。
